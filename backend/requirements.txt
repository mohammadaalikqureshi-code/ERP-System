# --- web framework ---------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- database --------------------------------------------------------------
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.1

# --- cache, queue, background jobs -----------------------------------------
redis[hiredis]==5.2.1
celery==5.4.0

# --- validation & settings -------------------------------------------------
pydantic[email]==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# --- security --------------------------------------------------------------
PyJWT==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1                     # passlib 1.7.4 reads bcrypt.__about__, removed in 4.1+
cryptography==44.0.0

# --- AI --------------------------------------------------------------------
# groq is the default assistant provider (OpenAI-compatible chat completions);
# anthropic is kept so a deployment can switch back with AI_PROVIDER=anthropic.
groq>=0.13,<1.0
anthropic==0.69.0

# --- documents & storage ---------------------------------------------------
weasyprint==63.1
boto3==1.36.2

# --- misc ------------------------------------------------------------------
httpx==0.28.1
pytz==2024.2

# --- testing ---------------------------------------------------------------
pytest==8.3.4
pytest-asyncio==0.25.2
aiosqlite==0.20.0
