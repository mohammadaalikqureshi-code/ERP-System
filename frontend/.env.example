# Copy to .env for local development.

# Where the API lives. A relative path lets the Vite dev proxy (and Nginx in
# production) forward requests, which avoids CORS entirely.
VITE_API_BASE_URL=/api/v1

# Product name shown in the sidebar and on the login screen.
VITE_APP_NAME=MediCare ERP
